# send2me Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Erstatt den gamle, feilende Anvil-appkoden i dette repoet med bookmarklet-løsningen fra spec-en (`docs/superpowers/specs/2026-07-24-send2me-design.md`).

**Architecture:** Anvil-app (git-synket til GitHub, Anvil deployer automatisk ved push). Ren logikk i `server_code/logic.py` (stdlib, testbar lokalt med unittest), tynn Anvil-lim i `server_code/api.py` (server-callables + HTTP-endepunkt), én form `RegisterForm`. Bookmarkleten genereres server-side (den må uansett inn i registrerings-eposten for mobilbruk).

**Tech Stack:** Anvil (python3-sandbox-runtime — kun stdlib på server), Anvil Data Tables, Anvil Email, Python 3 unittest lokalt.

## Global Constraints

- Branch: `master`. Committer per task, men **push først i Task 4** (Anvil deployer hver push — unngå halvferdige tilstander live).
- All UI- og e-posttekst på norsk.
- API-base: `https://send2me.app/_/api` (custom-domenet er koblet til appen).
- Rate-grense: maks 3 registrerings-eposter per adresse per dag.
- Rør ikke: `theme/`, `LICENSE.txt`, `.anvil_editor.yaml`, `.gitignore` (editor-metadata og tema gjenbrukes).
- `users`-tabellen beholdes i `db_schema` (unngår skjemakonflikt); Users-*tjenesten* fjernes.
- Serverkode: ingen eksterne avhengigheter; `secrets` med `uuid`-fallback for token.
- Testkommando: `python3 -m unittest discover -s tests -v` fra repo-roten.

---

### Task 1: Ren logikk (`logic.py`) med tester

**Files:**
- Create: `server_code/logic.py`
- Test: `tests/test_logic.py`

**Interfaces:**
- Produces (brukes av Task 2):
  - `normalize_email(s: str) -> str`
  - `valid_email(s: str) -> bool`
  - `next_reg_count(prev_date, prev_count, today, limit=3) -> int | None` (None = avvist)
  - `new_token() -> str`
  - `bookmarklet_js(token: str, api_base: str = API_BASE) -> str`
  - `registration_email_text(token: str, js: str) -> str`
  - `sent_page_html() -> str`
  - `error_page_html(msg: str) -> str`
  - Konstant `API_BASE = "https://send2me.app/_/api"`

- [ ] **Step 1: Skriv failende tester**

Opprett `tests/test_logic.py`:

```python
import sys
import unittest
import datetime
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "server_code"))
import logic


class TestEmail(unittest.TestCase):
    def test_normalize(self):
        self.assertEqual(logic.normalize_email("  Hans@GMAIL.com "), "hans@gmail.com")

    def test_valid(self):
        self.assertTrue(logic.valid_email("a@b.no"))
        self.assertFalse(logic.valid_email("ikke-epost"))
        self.assertFalse(logic.valid_email("a@b"))
        self.assertFalse(logic.valid_email(""))
        self.assertFalse(logic.valid_email("a b@c.no"))


class TestRateLimit(unittest.TestCase):
    today = datetime.date(2026, 7, 24)

    def test_first_ever(self):
        self.assertEqual(logic.next_reg_count(None, 0, self.today), 1)

    def test_new_day_resets(self):
        yesterday = datetime.date(2026, 7, 23)
        self.assertEqual(logic.next_reg_count(yesterday, 3, self.today), 1)

    def test_increments_same_day(self):
        self.assertEqual(logic.next_reg_count(self.today, 1, self.today), 2)

    def test_limit_reached(self):
        self.assertIsNone(logic.next_reg_count(self.today, 3, self.today))


class TestToken(unittest.TestCase):
    def test_token_long_and_unique(self):
        a, b = logic.new_token(), logic.new_token()
        self.assertGreaterEqual(len(a), 20)
        self.assertNotEqual(a, b)


class TestBookmarklet(unittest.TestCase):
    def test_contents(self):
        js = logic.bookmarklet_js("TOKEN123")
        self.assertTrue(js.startswith("javascript:"))
        self.assertIn("TOKEN123", js)
        self.assertIn("https://send2me.app/_/api/sendlink", js)
        self.assertIn("encodeURIComponent(location.href)", js)
        self.assertIn("encodeURIComponent(document.title)", js)
        self.assertIn("window.open", js)
        self.assertIn("location.href=u", js)  # popup-blokkering-fallback


class TestTexts(unittest.TestCase):
    def test_email_text(self):
        txt = logic.registration_email_text("TOK", "javascript:x")
        self.assertIn("TOK", txt)
        self.assertIn("javascript:x", txt)
        self.assertIn("https://send2me.app", txt)

    def test_sent_page(self):
        html = logic.sent_page_html()
        self.assertIn("Sendt", html)
        self.assertIn("window.close", html)
        self.assertIn("history.back", html)

    def test_error_page(self):
        self.assertIn("Ukjent", logic.error_page_html("Ukjent nøkkel"))


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: Kjør testene — de skal feile**

Kjør: `python3 -m unittest discover -s tests -v`
Forventet: FAIL/ERROR med `ModuleNotFoundError: No module named 'logic'`

- [ ] **Step 3: Implementér `server_code/logic.py`**

```python
"""Ren logikk for send2me - ingen anvil-avhengigheter, testbar lokalt."""
import re

API_BASE = "https://send2me.app/_/api"

_EMAIL_RE = re.compile(r"^[^@\s]+@[^@\s]+\.[^@\s]+$")

_BOOKMARKLET = (
    "javascript:(function(){var u='%s/sendlink?token=%s"
    "&url='+encodeURIComponent(location.href)+"
    "'&title='+encodeURIComponent(document.title);"
    "var w=window.open(u,'send2me','width=220,height=90,"
    "left='+(screen.width-250)+',top='+(screen.height-200));"
    "if(!w){location.href=u;}})()"
)


def normalize_email(s):
    return (s or "").strip().lower()


def valid_email(s):
    return bool(_EMAIL_RE.match(s or ""))


def next_reg_count(prev_date, prev_count, today, limit=3):
    """Ny reg_count for dagens registrering, eller None hvis grensen er nadd."""
    if prev_date != today:
        return 1
    if (prev_count or 0) >= limit:
        return None
    return prev_count + 1


def new_token():
    try:
        import secrets
        return secrets.token_urlsafe(18)
    except Exception:
        import uuid
        return uuid.uuid4().hex


def bookmarklet_js(token, api_base=API_BASE):
    return _BOOKMARKLET % (api_base, token)


def registration_email_text(token, js):
    return (
        "Hei!\n\n"
        "Her er din personlige send2me-nøkkel:\n\n"
        "    %s\n\n"
        "Slik kommer du i gang (PC/Mac):\n"
        "1. Gå til https://send2me.app/\n"
        "2. Lim inn nøkkelen i steg 2 og trykk «Lag bokmerke-lenke».\n"
        "3. Dra lenken som dukker opp, til bokmerkelinjen.\n\n"
        "På mobil:\n"
        "1. Lagre en hvilken som helst side som bokmerke, og gi det navnet send2me.\n"
        "2. Rediger bokmerket og erstatt adressen med hele koden under:\n\n"
        "%s\n\n"
        "Deretter: stå på en side, trykk på bokmerket — og lenken ligger i innboksen din.\n\n"
        "Hilsen send2me"
    ) % (token, js)


def sent_page_html():
    return (
        "<!doctype html><html><head><meta charset='utf-8'>"
        "<title>send2me</title></head>"
        "<body style='font-family:sans-serif;text-align:center;"
        "padding-top:1.6em;background:#fafafa'>"
        "<div style='font-size:1.5em'>&#10003; Sendt</div>"
        "<script>setTimeout(function(){window.close();"
        "if(history.length>1){history.back();}},900);</script>"
        "</body></html>"
    )


def error_page_html(msg):
    return (
        "<!doctype html><html><head><meta charset='utf-8'>"
        "<title>send2me</title></head>"
        "<body style='font-family:sans-serif;text-align:center;"
        "padding-top:1.6em;background:#fafafa'>"
        "<div style='font-size:1.2em'>&#10007; %s</div>"
        "</body></html>"
    ) % msg
```

- [ ] **Step 4: Kjør testene — alle skal passere**

Kjør: `python3 -m unittest discover -s tests -v`
Forventet: `OK` (12 tester)

- [ ] **Step 5: Commit**

```bash
git add server_code/logic.py tests/test_logic.py
git commit -m "feat: ren logikk for send2me med tester"
```

---

### Task 2: Anvil-lim (`api.py`), slett gammel servermodul

**Files:**
- Create: `server_code/api.py`
- Delete: `server_code/ServerModule1.py`

**Interfaces:**
- Consumes: alle funksjoner fra `logic` (Task 1).
- Produces (brukes av Task 3-formen):
  - server-callable `register_email(email: str) -> {"ok": bool, "error": str?}`
  - server-callable `make_bookmarklet(token: str) -> {"ok": bool, "js": str?, "error": str?}`
  - HTTP `GET /_/api/sendlink?token=..&url=..&title=..` → HTML-side

- [ ] **Step 1: Skriv `server_code/api.py`**

```python
import datetime

import anvil.email
import anvil.server
import anvil.tables as tables
from anvil.tables import app_tables

import logic


@anvil.server.callable
def register_email(email):
    email_n = logic.normalize_email(email)
    if not logic.valid_email(email_n):
        return {"ok": False, "error": "Ugyldig e-postadresse."}
    today = datetime.date.today()
    row = app_tables.subscribers.get(email=email_n)
    prev_date = row["reg_date"] if row else None
    prev_count = (row["reg_count"] if row else 0) or 0
    count = logic.next_reg_count(prev_date, prev_count, today)
    if count is None:
        return {"ok": False,
                "error": "For mange registreringer i dag. Prøv igjen i morgen."}
    token = logic.new_token()
    if row:
        row.update(token=token, reg_date=today, reg_count=count)
    else:
        app_tables.subscribers.add_row(
            email=email_n, token=token,
            created=datetime.datetime.now(),
            reg_date=today, reg_count=count)
    anvil.email.send(
        from_name="send2me", to=email_n,
        subject="Din send2me-nøkkel",
        text=logic.registration_email_text(token, logic.bookmarklet_js(token)))
    return {"ok": True}


@anvil.server.callable
def make_bookmarklet(token):
    token = (token or "").strip()
    row = app_tables.subscribers.get(token=token) if token else None
    if row is None:
        return {"ok": False,
                "error": "Ukjent nøkkel. Sjekk at du limte inn hele nøkkelen fra e-posten."}
    return {"ok": True, "js": logic.bookmarklet_js(token)}


def _html(body, status):
    resp = anvil.server.HttpResponse(status, body)
    resp.headers["Content-Type"] = "text/html; charset=utf-8"
    return resp


@anvil.server.http_endpoint("/sendlink")
def sendlink(**kwargs):
    params = anvil.server.request.query_params
    token = (params.get("token") or "").strip()
    url = params.get("url") or ""
    title = params.get("title") or ""
    row = app_tables.subscribers.get(token=token) if token else None
    if row is None:
        return _html(logic.error_page_html(
            "Ukjent nøkkel. Registrer deg på send2me.app."), 403)
    if not url:
        return _html(logic.error_page_html("Mangler URL."), 400)
    anvil.email.send(
        from_name="send2me", to=row["email"],
        subject=(title or url), text=url)
    return _html(logic.sent_page_html(), 200)
```

Merk: Anvil-servermoduler importerer hverandre med rent modulnavn (`import logic`) — ikke relativ import.

- [ ] **Step 2: Slett gammel modul og syntaks-sjekk**

```bash
git rm server_code/ServerModule1.py
python3 -m py_compile server_code/api.py && echo SYNTAX-OK
```
Forventet: `SYNTAX-OK` (py_compile importerer ikke anvil — bare syntaks).

- [ ] **Step 3: Kjør testene igjen (regresjon)**

Kjør: `python3 -m unittest discover -s tests -v`
Forventet: `OK`

- [ ] **Step 4: Commit**

```bash
git add server_code/api.py
git commit -m "feat: register/make_bookmarklet-callables og GET /sendlink; fjern gammel servermodul"
```

---

### Task 3: `anvil.yaml` + `RegisterForm`, slett `Form1`

**Files:**
- Modify: `anvil.yaml` (hele filen erstattes, innhold under)
- Create: `client_code/RegisterForm/__init__.py`
- Create: `client_code/RegisterForm/form_template.yaml`
- Delete: `client_code/Form1/` (begge filer)

**Interfaces:**
- Consumes: `register_email` og `make_bookmarklet` fra Task 2 (via `anvil.server.call`).
- Produces: startup-form `RegisterForm`; tabell `subscribers` med kolonnene
  `email` (string), `token` (string), `created` (datetime), `reg_date` (date),
  `reg_count` (number) — nøyaktig navnene Task 2 bruker.

- [ ] **Step 1: Erstatt `anvil.yaml` med:**

```yaml
allow_embedding: false
db_schema:
  subscribers:
    client: none
    columns:
    - admin_ui: {width: 200}
      name: email
      type: string
    - admin_ui: {width: 200}
      name: token
      type: string
    - admin_ui: {width: 150}
      name: created
      type: datetime
    - admin_ui: {width: 100}
      name: reg_date
      type: date
    - admin_ui: {width: 100}
      name: reg_count
      type: number
    indexes: []
    server: full
    title: Subscribers
  users:
    client: none
    columns:
    - admin_ui: {width: 200}
      client_hidden: null
      name: email
      type: string
    - admin_ui: {width: 100}
      client_hidden: null
      name: enabled
      type: bool
    indexes: []
    server: full
    title: Users
metadata: {description: 'Send lenken til siden du leser til din egen e-post - ett klikk.', logo_img: 'asset:iconfinder_telegram_3246742.png', title: send2me}
name: send2me
package_name: send2me
renamed: true
runtime_options:
  client_version: '3'
  server_spec: {}
  server_version: python3-sandbox
  version: 2
services:
- client_config: {}
  server_config: {}
  source: /runtime/services/anvil/email.yml
- client_config: {}
  server_config: {auto_create_missing_columns: false}
  source: /runtime/services/tables.yml
startup_form: RegisterForm
```

(Endringer fra original: `subscribers`-tabell lagt til, Users-tjenesten fjernet,
`startup_form: RegisterForm`, norsk beskrivelse. `users`-tabellen står igjen med vilje.)

- [ ] **Step 2: Opprett `client_code/RegisterForm/form_template.yaml`**

LinearPanel brukes som container (barna trenger ikke grid-posisjoner, i motsetning
til ColumnPanel — det gjør git-autorert yaml robust):

```yaml
components:
- components:
  - layout_properties: {}
    name: spacer_top
    properties: {height: 24}
    type: Spacer
  - layout_properties: {}
    name: label_title
    properties: {align: center, role: headline, text: send2me}
    type: Label
  - layout_properties: {}
    name: label_intro
    properties: {align: center, role: text, text: 'Send lenken til siden du leser til din egen e-post - ett klikk, ingen skriving.'}
    type: Label
  - layout_properties: {}
    name: label_step1
    properties: {role: subheading, text: '1. Registrer e-posten din'}
    type: Label
  - event_bindings: {pressed_enter: text_box_email_pressed_enter}
    layout_properties: {}
    name: text_box_email
    properties: {placeholder: 'din@epost.no', type: email}
    type: TextBox
  - event_bindings: {click: button_register_click}
    layout_properties: {}
    name: button_register
    properties: {role: primary-color, text: Registrer}
    type: Button
  - layout_properties: {}
    name: label_reg_status
    properties: {foreground: 'theme:Gray 600', text: ''}
    type: Label
  - layout_properties: {}
    name: label_step2
    properties: {role: subheading, text: '2. Lim inn nøkkelen fra e-posten'}
    type: Label
  - event_bindings: {pressed_enter: text_box_token_pressed_enter}
    layout_properties: {}
    name: text_box_token
    properties: {placeholder: 'nøkkelen du fikk tilsendt'}
    type: TextBox
  - event_bindings: {click: button_make_click}
    layout_properties: {}
    name: button_make
    properties: {role: primary-color, text: Lag bokmerke-lenke}
    type: Button
  - layout_properties: {}
    name: label_make_status
    properties: {foreground: 'theme:Gray 600', text: ''}
    type: Label
  - components: []
    layout_properties: {}
    name: link_bookmarklet
    properties: {align: center, bold: true, text: "✉️ send2me", tooltip: Dra meg til bokmerkelinjen, url: '', visible: false}
    type: Link
  - layout_properties: {}
    name: label_step3
    properties: {role: subheading, text: '3. Dra lenken over til bokmerkelinjen - ferdig! (På mobil: se oppskriften i e-posten.)', visible: false}
    type: Label
  - layout_properties: {}
    name: spacer_bottom
    properties: {height: 32}
    type: Spacer
  layout_properties: {slot: default}
  name: content_panel
  properties: {}
  type: LinearPanel
- layout_properties: {slot: title}
  name: label_apptitle
  properties: {align: left, role: headline, text: send2me}
  type: Label
container:
  properties: {html: '@theme:standard-page.html'}
  type: HtmlTemplate
is_package: true
```

(Filen er UTF-8 — norske tegn skrives direkte; tekster med komma/kolon står i
enkeltfnutter.)

- [ ] **Step 3: Opprett `client_code/RegisterForm/__init__.py`**

```python
from ._anvil_designer import RegisterFormTemplate
from anvil import *
import anvil.server


class RegisterForm(RegisterFormTemplate):

    def __init__(self, **properties):
        self.init_components(**properties)

    def button_register_click(self, **event_args):
        self.label_reg_status.text = "Sender ..."
        result = anvil.server.call('register_email', self.text_box_email.text or "")
        if result["ok"]:
            self.label_reg_status.text = "Sjekk innboksen din!"
        else:
            self.label_reg_status.text = result["error"]

    def text_box_email_pressed_enter(self, **event_args):
        self.button_register_click()

    def button_make_click(self, **event_args):
        self.label_make_status.text = ""
        result = anvil.server.call('make_bookmarklet', self.text_box_token.text or "")
        if result["ok"]:
            self.link_bookmarklet.url = result["js"]
            self.link_bookmarklet.visible = True
            self.label_step3.visible = True
        else:
            self.label_make_status.text = result["error"]

    def text_box_token_pressed_enter(self, **event_args):
        self.button_make_click()
```

- [ ] **Step 4: Slett Form1 og valider**

```bash
git rm -r client_code/Form1
python3 -m py_compile client_code/RegisterForm/__init__.py && echo SYNTAX-OK
python3 -c "import yaml" 2>/dev/null \
  && python3 -c "import yaml; yaml.safe_load(open('anvil.yaml')); yaml.safe_load(open('client_code/RegisterForm/form_template.yaml')); print('YAML-OK')" \
  || echo "PyYAML mangler - hopp over yaml-validering"
```
Forventet: `SYNTAX-OK` og `YAML-OK` (eller hopp-over-melding).

- [ ] **Step 5: Kjør testene igjen (regresjon)**

Kjør: `python3 -m unittest discover -s tests -v`
Forventet: `OK`

- [ ] **Step 6: Commit**

```bash
git add anvil.yaml client_code/RegisterForm
git commit -m "feat: RegisterForm på norsk, subscribers-skjema, fjern Users-tjeneste og Form1"
```

---

### Task 4: README, spec-justering, push og live-verifisering

**Files:**
- Modify: `README.md` (erstattes helt)
- Modify: `docs/superpowers/specs/2026-07-24-send2me-design.md` (én presisering)

**Interfaces:**
- Consumes: alt over; dette er utrullings- og dokumentasjonstasken.

- [ ] **Step 1: Erstatt `README.md` med:**

```markdown
# send2me

Send lenken til siden du leser til din egen e-post — ett klikk, ingen skriving.
Live på **https://send2me.app/**

## Slik virker det

1. Registrer e-postadressen din på send2me.app → du får en personlig nøkkel på e-post.
   (Nøkkelen vises aldri på skjermen: den som har nøkkelen, har bevist at de eier
   innboksen — og nøkkelen kan bare sende lenker til sin egen eier.)
2. Lim inn nøkkelen på siden og dra den genererte «send2me»-lenken til bokmerkelinjen.
3. Stå på en hvilken som helst side og klikk på bokmerket: den fullstendige URL-en
   sendes til deg. Et lite «✓ Sendt»-vindu lukker seg selv etter ett sekund.

Bookmarkleten åpner et vindu mot `GET /_/api/sendlink` i stedet for å bruke
`fetch` — dermed stopper ikke nettsiders CSP den, og URL-en sendes eksplisitt
som parameter (Referer-headeren inneholder bare domenet i moderne nettlesere,
som var feilen i forrige versjon av denne appen).

## Utvikling

- Repoet er git-synket med Anvil-appen: push til `master` → Anvil henter og
  deployer automatisk.
- Ren logikk ligger i `server_code/logic.py` (kun stdlib) og testes lokalt:
  `python3 -m unittest discover -s tests -v`
- Anvil-lim (server-callables + HTTP-endepunkt) i `server_code/api.py`,
  UI i `client_code/RegisterForm/`.
- Rate-grense: maks 3 registrerings-eposter per adresse per dag.
  Merk: Anvils gratisnivå har begrenset e-postkvote.

Design-spec og plan: `docs/superpowers/`.
```

- [ ] **Step 2: Presisér spec-en**

I spec-ens bookmarklet-seksjon: erstatt setningen om at genereringen skjer «rent
klientside» med at bookmarkleten genereres **server-side** (`logic.bookmarklet_js`)
— den må uansett inn i registrerings-eposten for mobiloppskriften, og siden
serveren allerede kjenner alle tokens, endrer ikke dette sikkerhetsmodellen.
Siden kaller `make_bookmarklet` for å få samme streng.

- [ ] **Step 3: Commit og push (første push — Anvil deployer)**

```bash
git add README.md docs/
git commit -m "docs: nytt README og spec-presisering"
git push
```

- [ ] **Step 4: Verifiser live**

1. Hent https://send2me.app/ (WebFetch): siden skal IKKE lenger inneholde
   «This app has experienced an error», og skal laste app-skallet med tittel send2me.
2. Hent `https://send2me.app/_/api/sendlink?token=finnesikke`:
   forventet «Ukjent nøkkel»-siden (HTTP 403) — beviser at endepunktet kjører.
3. Hvis siden fortsatt feiler: mest sannsynlige årsak er at Anvil-editoren venter
   på godkjenning av skjemaendringen (ny `subscribers`-tabell). Be Hans åpne appen
   i Anvil-editoren — den vil vise en «schema mismatch»-prompt som godtas med ett
   klikk. Dette er dokumentert i spec-en som kjent fallback.

- [ ] **Step 5: Menneskelig sluttest (Hans)**

1. Gå til https://send2me.app/, registrer din egen adresse.
2. Sjekk innboksen: e-post med nøkkel + mobiloppskrift.
3. Lim inn nøkkelen, trykk «Lag bokmerke-lenke», dra lenken til bokmerkelinjen.
4. Gå til en avisartikkel (f.eks. en vg.no-artikkel), klikk bokmerket.
5. Sjekk at e-posten inneholder **hele** artikkel-URL-en (ikke bare vg.no) og at
   emnet er sidetittelen.

---

## Self-review (utført ved skriving)

- Spec-dekning: registrering m/rate-grense ✓, token-epost ✓, bokmerke-generator ✓,
  GET /sendlink m/CSP-begrunnelse ✓, selvlukkende kvitteringsside ✓, norsk UI ✓,
  Users-tjeneste fjernet/users-tabell beholdt ✓, README-steg ✓, gjenbruk av tema ✓.
- Typer konsistente: kolonnenavn (`email/token/created/reg_date/reg_count`) og
  returformer (`{"ok": ...}`) er identiske i Task 1/2/3.
- Ingen plassholdere.
```
